import 'package:flutter/material.dart';

class RateAndDate extends StatelessWidget {
  RateAndDate({required this.rating, required this.size});
  double rating;
  double size;

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
