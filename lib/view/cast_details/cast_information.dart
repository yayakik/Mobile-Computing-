import 'package:flutter/material.dart';

class CastInformation extends StatelessWidget {
  const CastInformation({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold();
  }
}
